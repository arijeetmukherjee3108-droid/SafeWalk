<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_panel_rounded"
    android:orientation="horizontal"
    android:padding="14dp"
    android:gravity="center_vertical">

    <!-- Alert Type Icon -->
    <TextView
        android:id="@+id/tvAlertIcon"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:gravity="center"
        android:text="🚨"
        android:textSize="22sp"
        android:background="@drawable/bg_icon_ember" />

    <!-- Alert Content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvAlertTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SOS Alert"
            android:textColor="@color/text"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvAlertFrom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="From: User"
            android:textColor="@color/sub"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/tvAlertTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="2 min ago"
            android:textColor="@color/sub"
            android:textSize="11sp" />
    </LinearLayout>

    <!-- Status Badge -->
    <TextView
        android:id="@+id/tvAlertStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_blockchain_badge"
        android:paddingHorizontal="8dp"
        android:paddingVertical="3dp"
        android:text="SENT"
        android:textColor="@color/ember"
        android:textSize="10sp"
        android:textStyle="bold" />

</LinearLayout>
