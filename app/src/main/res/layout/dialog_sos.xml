<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_void"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Emergency Alert"
        android:textColor="@color/ember"
        android:textSize="30sp"
        android:textStyle="bold" />

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginVertical="32dp">

        <View
            android:id="@+id/pulseRing"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="center"
            android:alpha="0.2"
            android:background="@drawable/bg_sos_pulse" />

        <FrameLayout
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_sos_inner">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="SOS"
                android:textColor="@color/ember"
                android:textStyle="bold"
                android:textSize="24sp" />
        </FrameLayout>
    </FrameLayout>

    <!-- Status List -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp">

        <LinearLayout style="@style/SOSStatusRow">
            <View style="@style/SOSStatusDot" android:backgroundTint="@color/ember" />
            <TextView
                style="@style/SOSStatusLabel"
                android:text="Sending location to 3 contacts..." />
        </LinearLayout>

        <LinearLayout style="@style/SOSStatusRow">
            <View style="@style/SOSStatusDot" android:backgroundTint="@color/white" />
            <TextView
                style="@style/SOSStatusLabel"
                android:text="Resolving address..." />
        </LinearLayout>

        <LinearLayout style="@style/SOSStatusRow">
            <View style="@style/SOSStatusDot" android:backgroundTint="@color/white" />
            <TextView
                style="@style/SOSStatusLabel"
                android:text="Recording to blockchain..." />
        </LinearLayout>
    </LinearLayout>

    <Space
        android:layout_width="match_parent"
        android:layout_height="32dp" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/cancelButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:backgroundTint="@color/panel"
            android:text="False Alarm"
            android:textAllCaps="false"
            android:textColor="@color/text"
            app:cornerRadius="10dp"
            app:strokeColor="@color/border"
            app:strokeWidth="1dp" />

        <Button
            android:id="@+id/callPoliceButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:backgroundTint="@color/ember"
            android:text="Call Police"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textStyle="bold"
            app:cornerRadius="10dp" />
    </LinearLayout>

</LinearLayout>